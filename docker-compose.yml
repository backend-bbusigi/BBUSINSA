version: '3.8'

services:
  redis:
    container_name: bbusinsa_redis
    image: redis:7.4.2
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
      - ./docker/redis/conf/redis.conf:/usr/local/etc/redis/redis.conf

volumes:
  redis-data:
